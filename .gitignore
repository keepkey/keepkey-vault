node_modules/
dist/
build/
artifacts/
bun.lock
*.log
.DS_Store
.idea/

# Protobuf build artifacts
package/

# Icon upload temp state
scripts/.icon-upload-progress.json
scripts/.icon-cache/

# Credentials — NEVER commit
.env
.env.*
*.p12
*.cer
*.key
*.pem
*.mobileprovision

# Build artifacts
_build/
.make/
projects/keepkey-vault/_build/

# Unsigned firmware binaries
firmware/unsigned/

# Emulator binaries (built locally per channel — not committed)
firmware/emulators/7.14.0-*/
# Keep the legacy alpha for backward compat
!firmware/emulators/7.10.0-alpha/

# Emulator build directory
modules/keepkey-firmware/build-emu/
modules/keepkey-firmware/build-emu-*/

# Release-staged emulator libraries (rebuilt from the pinned firmware gitlink)
projects/keepkey-vault/emulator-bundle/*.dylib
projects/keepkey-vault/emulator-bundle/*.dll

# Release artifacts (belong in GitHub Releases, not the repo)
release-windows/

# Developer-local files
.claude/
emulator.img
rmdir-result.txt
*.bat
