{
  "name": "keepkey-vault-sdk",
  "version": "2.0.3",
  "description": "TypeScript SDK for KeepKey Vault REST API — zero dependencies, native fetch",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "files": ["lib"],
  "scripts": {
    "build": "tsc",
    "test": "tsc && node __tests__/test.js",
    "test:chain": "tsc && node tests/run-all.js chain",
    "test:sweep": "tsc && node tests/run-all.js sweep",
    "test:all": "tsc && node tests/run-all.js",
    "test:device": "tsc && node tests/run-all.js"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "keywords": ["keepkey", "hardware-wallet", "vault", "sdk"],
  "license": "MIT"
}
